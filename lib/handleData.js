const data = require('../data');

const database = {};

//create, read, update, delete

//get by id
//call back is the object
database.getAllData = (key, cb) => {
  if(data[key]){
    cb(null, )
  }
}
